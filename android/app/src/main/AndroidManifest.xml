<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!--
        Camera permission is requested at runtime by the web app
        (getUserMedia) once the user hits the scan page. Chrome relays
        the prompt; we declare it here so Android knows the app may
        request it. (Without this, Chrome's permission prompt is
        denied silently inside a TWA.)
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- For Bring!/n8n/etc. that the user may eventually link out to. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Pantria"
        android:supportsRtl="true"
        tools:targetApi="33">

        <!--
            Launcher activity. Subclasses
            com.google.androidbrowserhelper.trusted.LauncherActivity
            from the androidbrowserhelper library; we just point at the
            PWA URL via the meta-data block below.
        -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:label="@string/app_name">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/pantria_url" />

            <!--
                Status-bar / navigation-bar tint match the PWA theme
                colour so there's no visual seam between the system UI
                and the web content.
            -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/pantria_brand" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/pantria_brand" />

            <!--
                Splash screen shown while Chrome warms up. The drawable
                is rendered centred on the brand background.
            -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/pantria_brand" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep links: any https://<pantria_host>/... opens the TWA
                instead of the browser. Verified via the
                /.well-known/assetlinks.json file served by the Rails
                app (see android/README.md).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="@string/pantria_host" />
            </intent-filter>
        </activity>

        <!--
            Hand the digital-asset-links file location to the
            androidbrowserhelper at build time so the library can verify
            the relationship without an extra runtime hop.
        -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

    </application>
</manifest>

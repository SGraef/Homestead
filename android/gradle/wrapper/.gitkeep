# gradle-wrapper.jar lives here after `gradle wrapper --gradle-version 8.10.2`
# is run once (see android/README.md). We don't ship it pre-built so the repo
# never carries a binary from a third-party source.

<%= form_with model: @item, local: true do |f| %>
  <% if @item.errors.any? %>
    <div class="flash alert"><%= @item.errors.full_messages.to_sentence %></div>
  <% end %>
  <p><%= f.label :product_id %><br>
    <%= f.collection_select :product_id, current_household.products.order(:name), :id, :name %></p>
  <p><%= f.label :quantity %><br><%= f.number_field :quantity, step: 0.001, min: 0, required: true %></p>
  <p><%= f.label :location_id, t("storage.table.location") %><br>
    <%= f.collection_select :location_id, current_household.locations.ordered, :id, :name %></p>
  <p><%= f.label :expires_on %><br><%= f.date_field :expires_on %></p>
  <p><%= f.label :opened_on %><br><%= f.date_field :opened_on %></p>
  <p><%= f.label :frozen_on %><br><%= f.date_field :frozen_on %></p>
  <p><%= f.submit class: "btn" %></p>
<% end %>

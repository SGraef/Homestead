# frozen_string_literal: true
# typed: true

# Documents are shared household data; any member can add, view and remove them
# (same posture as receipts). Defaults from {ApplicationPolicy} are exactly
# that, so this is intentionally empty.
class DocumentPolicy < ApplicationPolicy
end
